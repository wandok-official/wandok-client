import { describe, it } from 'vitest';
// import { buildProgressSegments } from '../buildProgressSegments';

describe('buildProgressSegments', () => {
  it.todo('should build progress segments correctly');
});
